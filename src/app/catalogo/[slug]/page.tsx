import Link from 'next/link'; import {getProducts} from '@/lib/catalog'; import ProductCatalogCard from '@/components/ProductCatalogCard';
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getProducts(slug);return <main className="mx-auto min-h-screen max-w-6xl bg-[#f7f2eb] px-6 py-8"><Link href="/catalogo">← Todas as peças</Link><h1 className="my-8 text-center font-serif text-3xl">Peças da categoria</h1><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{p.map(x=><ProductCatalogCard product={x} key={x.id}/>)}</div></main>}
