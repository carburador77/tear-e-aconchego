'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { createClient } from '@/lib/supabase/client';
export default function Login(){const router=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else{await supabase.auth.getSession();router.replace('/admin');router.refresh()}setLoading(false)}return <main className="grid min-h-screen place-items-center bg-[#f2ece3] p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-xl bg-[#fffdf9] p-7 shadow"><h1 className="font-serif text-3xl text-[#52604a]">Área administrativa</h1><p className="mb-6 mt-2 text-sm">Entre para administrar o catálogo.</p><label className="block text-sm">E-mail<input className="mt-1 w-full rounded border p-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-4 block text-sm">Senha<input className="mt-1 w-full rounded border p-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}<button className="mt-6 w-full rounded bg-[#52604a] p-3 text-sm font-bold text-white" disabled={loading}>{loading?'ENTRANDO…':'ENTRAR'}</button></form></main>}
