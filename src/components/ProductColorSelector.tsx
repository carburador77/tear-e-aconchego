'use client';
import {useState} from 'react';import type {ProductVariant} from '@/types/catalog';
export default function ProductColorSelector({image,variants}:{image:string;variants:ProductVariant[]}){const[selected,setSelected]=useState(variants[0]);const current=selected?.image_url||image;return <div><img className="h-auto max-h-[560px] w-full object-contain transition-opacity duration-200" src={current} alt="Produto"/>{selected&&<div className="mt-4"><p className="text-sm">Cor: <strong>{selected.color_name}</strong></p><div className="mt-3 flex gap-3">{variants.map(v=><button key={v.id} type="button" title={v.color_name} aria-label={`Selecionar ${v.color_name}`} onClick={()=>setSelected(v)} className={`h-8 w-8 cursor-pointer rounded-full border border-[#8d8173] ${selected.id===v.id?'ring-2 ring-[#52604a] ring-offset-2':''}`} style={{backgroundColor:v.color_hex}}/>)}</div></div>}</div>}
